/**
 * author: JSサポーターズ
 * リスト1: Webストレージへデータを保存する
 */

// プロパティへのアクセスと同じように利用可能
localStorage.foo = 'bar';

// setItemメソッドも利用可能
localStorage.setItem('foo', 'bar');

// セッションストレージも同じ方法で利用可能
sessionStorage.foo = 'bar';
sessionStorage.setItem('foo', 'bar');

